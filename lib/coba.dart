import 'package:flutter/material.dart';

void main() {
  runApp(const MyTry());
}

class MyTry extends StatelessWidget {
  const MyTry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
